TO DOs
- Landing (página de inicio) con datos salientes de base de datos [Izhan]
- Sistema de email para que los padres contacten con nosotros (formulario) [Izhan]
- Galeria
	- Subir más de una foto a la vez (interno)
	- Poner contraseñas a las páginas
	- Interfaz
		- Páginas y subpáginas (división)
		- Forma de ver las fotos
- Contacto (html)
- Monitores
	- Cartas de monis responsive [Javi]
	- Revisar CSS que interfiere -> bootstrap y se soluciona más tarde


class Post(models.Model):
    title = models.CharField(max_length=250)
    description = models.TextField()
    image = models.FileField(blank=True)
 
    def __str__(self):
        return self.title
 
class PostImage(models.Model):
    post = models.ForeignKey(Post, default=None, on_delete=models.CASCADE)
    images = models.FileField(upload_to = 'images/')
 
    def __str__(self):
        return self.post.title

